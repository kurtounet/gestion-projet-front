import { GetPriorityPipe } from './get-priority-pipe';

describe('GetPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
